為您梳理並總結先前討論的所有核心技術痛點與改善機會點。以下為您量身打造一個「工業級 AI 控制優化 Canvas 畫布」，將您提出的 8 個實務問題（包含非自迴歸輸入特徵、分佈偏移、手動 SafeBO、PINN 物理約束、雙時間尺度卡爾曼濾波等）系統性地歸納為 7 大改善機會點，並提供具體的落地作法與技術步驟。

---

### pH 預測控制與貝氏最佳化改善機會點 Canvas 畫布
1. **1. 物理化學 (滴定非線性)**
單一組 PID 參數無法適應 pH 滴定曲線中平緩區（低增益）與中性敏感區（極高增益）之間高達千萬倍的增益變化。
    (1) 系統在接近中性點 (pH 6-8) 時劇烈震盪，但在遠離中性點時響應極度遲緩。
    (2) 導入增益排程 (Gain Scheduling) 的多區域分治控制策略。
    (3) 根據滴定特徵將 pH 操作範圍切分為多個子區域，分別利用 BO 在不同操作區段下尋找專屬的局部 PID 參數 (Kp, Ki, Kd)。

2. **2. 模型預測 (過度平滑與漂移)**
LSTM 預測時會平滑陡峭的當量點轉折，且前饋開迴路模擬缺乏出口 pH 反饋，導致累積誤差隨時間指數級放大。
    (1) 比較「開迴路訓練」與「多步自體遞迴」預測。若長時域閉迴路預測的出口 pH 隨時間嚴重漂移，即代表模型存在漂移。
    (2) 導入物理知識限制 (PINN) + 線上偏差校正 (Bias Correction)。 
        (2.1) 在 LSTM 的 Loss 函數中加入一階導數符號限制與出口邊界約束。
        (2.2) 實作卡爾曼濾波器，利用每秒實際出口 pH 與預測值的殘差，即時修正預測基線。

3. **3. 演算法漏洞 (分佈偏移與利用)**
BO 會生成極端或高頻的控制訊號 (閥門開度)，將 LSTM 推入未見過的分佈外區域 (OOD) 並鑽模型漏洞，選出「虛假最佳參數」。
    (1) 訓練一個傾向分數模型 (二元分類器)，區分「歷史訓練特徵」與「BO 測試特徵」。若分類器 AUC 顯著大於 0.5 且接近 1.0，即發生偏移。
    (2) 採用手動控制迴圈 (Manual Loop) 進行安全約束貝氏最佳化 (Safe BO)。
    (3) 改寫 bayes_opt 套件。利用 optimizer.suggest 取得參數後，先由高斯過程的預測標準差檢測不確定性。若 std 超出閾值，直接註冊極差分數拒絕此點，不送入 LSTM。

4. **4. 系統觀測 (異質變異數不匹配)**
傳統 BO 預設全域雜訊恆定，但 pH 系統在中性區的流體量測雜訊大、酸鹼區小，導致 BO 在敏感區過度擬合。
    (1) 統計不同 pH 區間下歷史實測數據的標準差，若中性區的變異數明顯大於兩端，即屬於異質變異數特徵。
    (2) 採用異質貝氏最佳化 (Heteroscedastic BO)。

5. **5. 系統實務 (混合死區時間)**
反應系統存在物料混合與傳輸的純時間延遲。LSTM 雖能接收時間序列，但極難自學隨流量動態變化的延遲。
    (1) 給藥動作與感測器 pH 發生變化之間存在數秒至數十秒的時間差，且該延遲時間與當前流量大小成反比。
    (2) 導入動態時間軸平移 (Smith Predictor 補償概念)。
    (3) 在特徵工程階段，依據物理公式計算出即時延遲時間，並將「閥開度」特徵在時間軸上主動往回平移對齊出口 pH，免除 LSTM 記憶物理延遲的負擔。

6. **6. 系統實務 (目標函數單一)**
若僅最小化 pH 誤差，BO 為了壓低預測誤差會容許控制器頻繁切換閥門，產生不切實際的高頻震盪 (Jitter)。
    (1) 觀察優化後的模擬 PID 控制輸出 (PID OUT)，閥門開度特徵呈現極高頻的劇烈鋸齒狀或大範圍切換。
    (2) 設計帶控制量懲罰的複合目標函數 (Composite Cost Function)。
    (3) 將 BO 目標函數修改為同時懲罰控制誤差與閥門動作變化率，強迫 BO 避開高頻切換的盲區，選出平滑的控制參數。

7. **7. 系統實務 (雙時間尺度)**
系統預測與控制是秒級動態，但 PID 參數為每班 (8小時) 更新一次，時間尺度不匹配導致部署困難。
    (1) 製程流量與入口 pH 隨時間緩慢改變，固定參數無法應對下一班的工況，而頻繁線上更新又容易造成系統切換衝擊。
    (2) 實施雙時間尺度 (Two-Scale) 操作與優化機制。
        (2-1) 秒級尺度：卡爾曼濾波器線上即時修正 LSTM 當前狀態，確保虛擬環境精準。
        (2-2) 班級尺度：每 8 小時交班前，在已校正的虛擬環境中跑數千次 BO 模擬，計算出下一班參數並部署。

---

### 核心技術落地深挖與數學實現

#### 一、 分佈偏移（Covariate Shift）的檢測與校正

為了確保您的驗證集與測試集分布一致，或是測量 BO 產生的虛擬特徵與歷史資料有多大的偏離，您可以訓練一個傾向分數模型（Propensity Model）$M_p$。

##### 1. 檢測機制 (Classifier-Based)

將歷史訓練特徵資料標記為 $y=0$，將 BO 優化過程中產生的控制特徵標記為 $y=1$。訓練一個二元分類器（如 XGBoost）來預測樣本來源：


$$P(y=1|x) = M_p(x)$$

* **若 $AUC \approx 0.5$**：代表兩者特徵分佈一致，LSTM 在其熟悉的領域預測，結果高度可信。


* **若 $AUC \gg 0.5$（例如 $AUC > 0.8$）**：發生嚴重分佈偏移，BO 正在探索 LSTM 沒見過的控制訊號（OOD），此時 LSTM 的預測極不可信，必須觸發安全防禦機制。



##### 2. 預防與重加權 (IPW)

為了修正分布差異對評估預測誤差的影響，使用逆機率加權（Inverse Probability Weighting）為驗證集中的每筆資料賦予連續權重 $w_i$：


$$w_i = \frac{M_p(x_i)}{1 - M_p(x_i)}$$


這能重新調整驗證集的樣本權重，使其在數學上完美對齊目標測試集的聯合分佈，協助您計算出模型在未來真實環境下的真實預期誤差。

---

#### 二、 防止對抗性利用：在 `BayesianOptimization` 中實作「安全不確定性約束」

由於 `pbounds` 僅能限制靜態的參數上下界（如 $K_p \in [0.1, 10]$），無法防止 BO 鑽 LSTM 在合法範圍內的預測盲區。因此，必須使用**手動控制迴圈 (Manual Loop)** 來動態拒絕不確定性過高的參數：

```python
from bayes_opt import BayesianOptimization
from bayes_opt import UtilityFunction

# 1. 宣告優化器（不傳入目標函數，手動接管控制流）
optimizer = BayesianOptimization(
    f=None,
    pbounds={"Kp": (0.1, 10.0), "Ki": (0.01, 2.0), "Kd": (0.0, 1.0)},
    random_state=42
)

utility = UtilityFunction(kind="ucb", kappa=1.96, xi=0.01)

# 2. 迭代尋優
for _ in range(50):
    next_point = optimizer.suggest(utility)
    
    # 3. 從底層高斯過程 (GP) 提取不確定性 (預測標準差 std)
    # 我們需要將建議參數轉換為 numpy 陣列進行 predict
    x_array = optimizer._space.params_to_array(next_point).reshape(1, -1)
    
    # 利用內建的 GP 模型預測該點的平均值與標準差
    mean, std = optimizer._gp.predict(x_array, return_std=True)
    
    # 4. 安全審查機制 (Safe BO)
    # 若 std 超過安全臨界值值，代表此參數落入 LSTM 的未知盲區 (OOD)
    if std[0] > 0.5:  # 0.5 為假設的標準差閾值
        # 拒絕此點，直接給予極差的懲罰分數 (例如 -9999)，並註冊回 GP 中
        # 這會教育高斯過程「這是一個危險/無效區域」，下次不要再來探索
        optimizer.register(params=next_point, target=-9999.0)
    else:
        # 5. 安全通過，執行 LSTM 模擬並計算真實適應度
        score = evaluate_pid_on_lstm_simulator(next_point) 
        optimizer.register(params=next_point, target=score)

```

---

#### 三、 物理約束 (PINN) 損失函數設計

為了消除 LSTM 在非自迴歸輸入下對滴定曲線產生的過度平滑效應，並防止模型給出不合物理邏輯的預測，您可以在模型的 Loss 函數中加入**單調性物理約束與一階導數符號限制**：

##### 1. 單調性一階導數限制

酸閥開度（$u_{\text{acid}}$）增加，pH 預測值（$\hat{y}$）必須下降；鹼閥開度（$u_{\text{base}}$）增加，pH 預測值（$\hat{y}$）必須上升。我們將此物理邏輯轉化為偏微分懲罰項並寫入訓練損失中：


$$\mathcal{L}_{\text{physics}} = \max\left(0, \frac{\partial \hat{y}_{t+1}}{\partial u_{\text{acid}, t}}\right) + \max\left(0, -\frac{\partial \hat{y}_{t+1}}{\partial u_{\text{base}, t}}\right)$$

##### 2. 質量守恆與邊界極限

預測的出口 pH 值絕對不可能超出強酸原液的極限酸度 $pH_{\text{min}}$ 與強鹼原液的極限鹼度 $pH_{\text{max}}$：


$$\mathcal{L}_{\text{boundary}} = \max\left(0, pH_{\text{min}} - \hat{y}_{t+1}\right) + \max\left(0, \hat{y}_{t+1} - pH_{\text{max}}\right)$$

##### 3. 最終複合訓練 Loss

$$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{MSE}} + \alpha \mathcal{L}_{\text{physics}} + \beta \mathcal{L}_{\text{boundary}}$$

為您梳理並總結先前討論的所有核心技術痛點與改善機會點。以下為您量身打造一個「工業級 AI 控制優化 Canvas 畫布」，將您提出的 8 個實務問題（包含非自迴歸輸入特徵、分佈偏移、手動 SafeBO、PINN 物理約束、雙時間尺度卡爾曼濾波等）系統性地歸納為 7 大改善機會點，並提供具體的落地作法與技術步驟。

---

### pH 預測控制與貝氏最佳化改善機會點 Canvas 畫布

*註：為符合排版與系統規範，下表內容不含 LaTeX 數學公式。公式細節與原理將在畫布下方的「核心技術深挖」章節中以標準數學公式為您詳細拆解。*

| 改善機會點維度 | 核心痛點與模型盲區 | 檢測與診斷機制 | 具體落地改進作法 | 實作步驟與工具建議 |
| --- | --- | --- | --- | --- |
| **1. 物理化學 (滴定非線性)** | 單一組 PID 參數無法適應 pH 滴定曲線中平緩區（低增益）與中性敏感區（極高增益）之間高達千萬倍的增益變化。

 | 系統在接近中性點 (pH 6-8) 時劇烈震盪，但在遠離中性點時響應極度遲緩。

 | 導入增益排程 (Gain Scheduling) 的多區域分治控制策略。

 | 根據滴定特徵將 pH 操作範圍切分為多個子區域，分別利用 BO 在不同操作區段下尋找專屬的局部 PID 參數 (Kp, Ki, Kd)。

 |
| **2. 模型預測 (過度平滑與漂移)** | LSTM 預測時會平滑陡峭的當量點轉折，且前饋開迴路模擬缺乏出口 pH 反饋，導致累積誤差隨時間指數級放大。

 | 比較「開迴路訓練」與「多步自體遞迴」預測。若長時域閉迴路預測的出口 pH 隨時間嚴重漂移，即代表模型存在漂移。

 | 導入物理知識限制 (PINN) + 線上偏差校正 (Bias Correction)。

 | 1. 在 LSTM 的 Loss 函數中加入一階導數符號限制與出口邊界約束。 2. 實作卡爾曼濾波器，利用每秒實際出口 pH 與預測值的殘差，即時修正預測基線。

 |
| **3. 演算法漏洞 (分佈偏移與利用)** | BO 會生成極端或高頻的控制訊號 (閥門開度)，將 LSTM 推入未見過的分佈外區域 (OOD) 並鑽模型漏洞，選出「虛假最佳參數」。

 | 訓練一個傾向分數模型 (二元分類器)，區分「歷史訓練特徵」與「BO 測試特徵」。若分類器 AUC 顯著大於 0.5 且接近 1.0，即發生偏移。

 | 採用手動控制迴圈 (Manual Loop) 進行安全約束貝氏最佳化 (Safe BO)。

 | 改寫 bayes_opt 套件。利用 optimizer.suggest 取得參數後，先由高斯過程的預測標準差檢測不確定性。若 std 超出閾值，直接註冊極差分數拒絕此點，不送入 LSTM。

 |
| **4. 系統觀測 (異質變異數不匹配)** | 傳統 BO 預設全域雜訊恆定，但 pH 系統在中性區的流體量測雜訊大、酸鹼區小，導致 BO 在敏感區過度擬合。

 | 統計不同 pH 區間下歷史實測數據的標準差，若中性區的變異數明顯大於兩端，即屬於異質變異數特徵。

 | 採用異質貝氏最佳化 (Heteroscedastic BO)。

 | 將 BO 框架更換至 BoTorch，底層改用 HeteroskedasticSingleTaskGP 模型，同時對預測值與輸入相關的雜訊變異數建模，避免 BO 受到高雜訊誤導。

 |
| **5. 系統實務 (混合死區時間)** | 反應系統存在物料混合與傳輸的純時間延遲。LSTM 雖能接收時間序列，但極難自學隨流量動態變化的延遲。

 | 給藥動作與感測器 pH 發生變化之間存在數秒至數十秒的時間差，且該延遲時間與當前流量大小成反比。

 | 導入動態時間軸平移 (Smith Predictor 補償概念)。

 | 在特徵工程階段，依據物理公式計算出即時延遲時間，並將「閥開度」特徵在時間軸上主動往回平移對齊出口 pH，免除 LSTM 記憶物理延遲的負擔。

 |
| **6. 系統實務 (目標函數單一)** | 若僅最小化 pH 誤差，BO 為了壓低預測誤差會容許控制器頻繁切換閥門，產生不切實際的高頻震盪 (Jitter)。

 | 觀察優化後的模擬 PID 控制輸出 (PID OUT)，閥門開度特徵呈現極高頻的劇烈鋸齒狀或大範圍切換。

 | 設計帶控制量懲罰的複合目標函數 (Composite Cost Function)。

 | 將 BO 目標函數修改為同時懲罰控制誤差與閥門動作變化率，強迫 BO 避開高頻切換的盲區，選出平滑的控制參數。

 |
| **7. 系統實務 (雙時間尺度)** | 系統預測與控制是秒級動態，但 PID 參數為每班 (8小時) 更新一次，時間尺度不匹配導致部署困難。

 | 製程流量與入口 pH 隨時間緩慢改變，固定參數無法應對下一班的工況，而頻繁線上更新又容易造成系統切換衝擊。

 | 實施雙時間尺度 (Two-Scale) 操作與優化機制。

 | 1. 秒級尺度：卡爾曼濾波器線上即時修正 LSTM 當前狀態，確保虛擬環境精準。 2. 班級尺度：每 8 小時交班前，在已校正的虛擬環境中跑數千次 BO 模擬，計算出下一班參數並部署。

 |

---

### 核心技術落地深挖與數學實現

#### 一、 分佈偏移（Covariate Shift）的檢測與校正

為了確保您的驗證集與測試集分布一致，或是測量 BO 產生的虛擬特徵與歷史資料有多大的偏離，您可以訓練一個傾向分數模型（Propensity Model）$M_p$。

##### 1. 檢測機制 (Classifier-Based)

將歷史訓練特徵資料標記為 $y=0$，將 BO 優化過程中產生的控制特徵標記為 $y=1$。訓練一個二元分類器（如 XGBoost）來預測樣本來源：


$$P(y=1|x) = M_p(x)$$

* **若 $AUC \approx 0.5$**：代表兩者特徵分佈一致，LSTM 在其熟悉的領域預測，結果高度可信。


* **若 $AUC \gg 0.5$（例如 $AUC > 0.8$）**：發生嚴重分佈偏移，BO 正在探索 LSTM 沒見過的控制訊號（OOD），此時 LSTM 的預測極不可信，必須觸發安全防禦機制。



##### 2. 預防與重加權 (IPW)

為了修正分布差異對評估預測誤差的影響，使用逆機率加權（Inverse Probability Weighting）為驗證集中的每筆資料賦予連續權重 $w_i$：


$$w_i = \frac{M_p(x_i)}{1 - M_p(x_i)}$$


這能重新調整驗證集的樣本權重，使其在數學上完美對齊目標測試集的聯合分佈，協助您計算出模型在未來真實環境下的真實預期誤差。

---

#### 二、 防止對抗性利用：在 `BayesianOptimization` 中實作「安全不確定性約束」
為您梳理並總結先前討論的所有核心技術痛點與改善機會點。以下為您量身打造一個「工業級 AI 控制優化 Canvas 畫布」，將您提出的 8 個實務問題（包含非自迴歸輸入特徵、分佈偏移、手動 SafeBO、PINN 物理約束、雙時間尺度卡爾曼濾波等）系統性地歸納為 7 大改善機會點，並提供具體的落地作法與技術步驟。

---

### pH 預測控制與貝氏最佳化改善機會點 Canvas 畫布

*註：為符合排版與系統規範，下表內容不含 LaTeX 數學公式。公式細節與原理將在畫布下方的「核心技術深挖」章節中以標準數學公式為您詳細拆解。*

| 改善機會點維度 | 核心痛點與模型盲區 | 檢測與診斷機制 | 具體落地改進作法 | 實作步驟與工具建議 |
| --- | --- | --- | --- | --- |
| **1. 物理化學 (滴定非線性)** | 單一組 PID 參數無法適應 pH 滴定曲線中平緩區（低增益）與中性敏感區（極高增益）之間高達千萬倍的增益變化。

 | 系統在接近中性點 (pH 6-8) 時劇烈震盪，但在遠離中性點時響應極度遲緩。

 | 導入增益排程 (Gain Scheduling) 的多區域分治控制策略。

 | 根據滴定特徵將 pH 操作範圍切分為多個子區域，分別利用 BO 在不同操作區段下尋找專屬的局部 PID 參數 (Kp, Ki, Kd)。

 |
| **2. 模型預測 (過度平滑與漂移)** | LSTM 預測時會平滑陡峭的當量點轉折，且前饋開迴路模擬缺乏出口 pH 反饋，導致累積誤差隨時間指數級放大。

 | 比較「開迴路訓練」與「多步自體遞迴」預測。若長時域閉迴路預測的出口 pH 隨時間嚴重漂移，即代表模型存在漂移。

 | 導入物理知識限制 (PINN) + 線上偏差校正 (Bias Correction)。

 | 1. 在 LSTM 的 Loss 函數中加入一階導數符號限制與出口邊界約束。 2. 實作卡爾曼濾波器，利用每秒實際出口 pH 與預測值的殘差，即時修正預測基線。

 |
| **3. 演算法漏洞 (分佈偏移與利用)** | BO 會生成極端或高頻的控制訊號 (閥門開度)，將 LSTM 推入未見過的分佈外區域 (OOD) 並鑽模型漏洞，選出「虛假最佳參數」。

 | 訓練一個傾向分數模型 (二元分類器)，區分「歷史訓練特徵」與「BO 測試特徵」。若分類器 AUC 顯著大於 0.5 且接近 1.0，即發生偏移。

 | 採用手動控制迴圈 (Manual Loop) 進行安全約束貝氏最佳化 (Safe BO)。

 | 改寫 bayes_opt 套件。利用 optimizer.suggest 取得參數後，先由高斯過程的預測標準差檢測不確定性。若 std 超出閾值，直接註冊極差分數拒絕此點，不送入 LSTM。

 |
| **4. 系統觀測 (異質變異數不匹配)** | 傳統 BO 預設全域雜訊恆定，但 pH 系統在中性區的流體量測雜訊大、酸鹼區小，導致 BO 在敏感區過度擬合。

 | 統計不同 pH 區間下歷史實測數據的標準差，若中性區的變異數明顯大於兩端，即屬於異質變異數特徵。

 | 採用異質貝氏最佳化 (Heteroscedastic BO)。

 | 將 BO 框架更換至 BoTorch，底層改用 HeteroskedasticSingleTaskGP 模型，同時對預測值與輸入相關的雜訊變異數建模，避免 BO 受到高雜訊誤導。

 |
| **5. 系統實務 (混合死區時間)** | 反應系統存在物料混合與傳輸的純時間延遲。LSTM 雖能接收時間序列，但極難自學隨流量動態變化的延遲。

 | 給藥動作與感測器 pH 發生變化之間存在數秒至數十秒的時間差，且該延遲時間與當前流量大小成反比。

 | 導入動態時間軸平移 (Smith Predictor 補償概念)。

 | 在特徵工程階段，依據物理公式計算出即時延遲時間，並將「閥開度」特徵在時間軸上主動往回平移對齊出口 pH，免除 LSTM 記憶物理延遲的負擔。

 |
| **6. 系統實務 (目標函數單一)** | 若僅最小化 pH 誤差，BO 為了壓低預測誤差會容許控制器頻繁切換閥門，產生不切實際的高頻震盪 (Jitter)。

 | 觀察優化後的模擬 PID 控制輸出 (PID OUT)，閥門開度特徵呈現極高頻的劇烈鋸齒狀或大範圍切換。

 | 設計帶控制量懲罰的複合目標函數 (Composite Cost Function)。

 | 將 BO 目標函數修改為同時懲罰控制誤差與閥門動作變化率，強迫 BO 避開高頻切換的盲區，選出平滑的控制參數。

 |
| **7. 系統實務 (雙時間尺度)** | 系統預測與控制是秒級動態，但 PID 參數為每班 (8小時) 更新一次，時間尺度不匹配導致部署困難。

 | 製程流量與入口 pH 隨時間緩慢改變，固定參數無法應對下一班的工況，而頻繁線上更新又容易造成系統切換衝擊。

 | 實施雙時間尺度 (Two-Scale) 操作與優化機制。

 | 1. 秒級尺度：卡爾曼濾波器線上即時修正 LSTM 當前狀態，確保虛擬環境精準。 2. 班級尺度：每 8 小時交班前，在已校正的虛擬環境中跑數千次 BO 模擬，計算出下一班參數並部署。

 |

---

### 核心技術落地深挖與數學實現

#### 一、 分佈偏移（Covariate Shift）的檢測與校正

為了確保您的驗證集與測試集分布一致，或是測量 BO 產生的虛擬特徵與歷史資料有多大的偏離，您可以訓練一個傾向分數模型（Propensity Model）$M_p$。

##### 1. 檢測機制 (Classifier-Based)

將歷史訓練特徵資料標記為 $y=0$，將 BO 優化過程中產生的控制特徵標記為 $y=1$。訓練一個二元分類器（如 XGBoost）來預測樣本來源：


$$P(y=1|x) = M_p(x)$$

* **若 $AUC \approx 0.5$**：代表兩者特徵分佈一致，LSTM 在其熟悉的領域預測，結果高度可信。


* **若 $AUC \gg 0.5$（例如 $AUC > 0.8$）**：發生嚴重分佈偏移，BO 正在探索 LSTM 沒見過的控制訊號（OOD），此時 LSTM 的預測極不可信，必須觸發安全防禦機制。



##### 2. 預防與重加權 (IPW)

為了修正分布差異對評估預測誤差的影響，使用逆機率加權（Inverse Probability Weighting）為驗證集中的每筆資料賦予連續權重 $w_i$：


$$w_i = \frac{M_p(x_i)}{1 - M_p(x_i)}$$


這能重新調整驗證集的樣本權重，使其在數學上完美對齊目標測試集的聯合分佈，協助您計算出模型在未來真實環境下的真實預期誤差。

---

#### 二、 防止對抗性利用：在 `BayesianOptimization` 中實作「安全不確定性約束」

由於 `pbounds` 僅能限制靜態的參數上下界（如 $K_p \in [0.1, 10]$），無法防止 BO 鑽 LSTM 在合法範圍內的預測盲區。因此，必須使用**手動控制迴圈 (Manual Loop)** 來動態拒絕不確定性過高的參數：

```python
from bayes_opt import BayesianOptimization
from bayes_opt import UtilityFunction

# 1. 宣告優化器（不傳入目標函數，手動接管控制流）
optimizer = BayesianOptimization(
    f=None,
    pbounds={"Kp": (0.1, 10.0), "Ki": (0.01, 2.0), "Kd": (0.0, 1.0)},
    random_state=42
)

utility = UtilityFunction(kind="ucb", kappa=1.96, xi=0.01)

# 2. 迭代尋優
for _ in range(50):
    next_point = optimizer.suggest(utility)
    
    # 3. 從底層高斯過程 (GP) 提取不確定性 (預測標準差 std)
    # 我們需要將建議參數轉換為 numpy 陣列進行 predict
    x_array = optimizer._space.params_to_array(next_point).reshape(1, -1)
    
    # 利用內建的 GP 模型預測該點的平均值與標準差
    mean, std = optimizer._gp.predict(x_array, return_std=True)
    
    # 4. 安全審查機制 (Safe BO)
    # 若 std 超過安全臨界值值，代表此參數落入 LSTM 的未知盲區 (OOD)
    if std[0] > 0.5:  # 0.5 為假設的標準差閾值
        # 拒絕此點，直接給予極差的懲罰分數 (例如 -9999)，並註冊回 GP 中
        # 這會教育高斯過程「這是一個危險/無效區域」，下次不要再來探索
        optimizer.register(params=next_point, target=-9999.0)
    else:
        # 5. 安全通過，執行 LSTM 模擬並計算真實適應度
        score = evaluate_pid_on_lstm_simulator(next_point) 
        optimizer.register(params=next_point, target=score)

```

---

#### 三、 物理約束 (PINN) 損失函數設計

為了消除 LSTM 在非自迴歸輸入下對滴定曲線產生的過度平滑效應，並防止模型給出不合物理邏輯的預測，您可以在模型的 Loss 函數中加入**單調性物理約束與一階導數符號限制**：

##### 1. 單調性一階導數限制

酸閥開度（$u_{\text{acid}}$）增加，pH 預測值（$\hat{y}$）必須下降；鹼閥開度（$u_{\text{base}}$）增加，pH 預測值（$\hat{y}$）必須上升。我們將此物理邏輯轉化為偏微分懲罰項並寫入訓練損失中：


$$\mathcal{L}_{\text{physics}} = \max\left(0, \frac{\partial \hat{y}_{t+1}}{\partial u_{\text{acid}, t}}\right) + \max\left(0, -\frac{\partial \hat{y}_{t+1}}{\partial u_{\text{base}, t}}\right)$$

##### 2. 質量守恆與邊界極限

預測的出口 pH 值絕對不可能超出強酸原液的極限酸度 $pH_{\text{min}}$ 與強鹼原液的極限鹼度 $pH_{\text{max}}$：


$$\mathcal{L}_{\text{boundary}} = \max\left(0, pH_{\text{min}} - \hat{y}_{t+1}\right) + \max\left(0, \hat{y}_{t+1} - pH_{\text{max}}\right)$$

##### 3. 最終複合訓練 Loss

$$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{MSE}} + \alpha \mathcal{L}_{\text{physics}} + \beta \mathcal{L}_{\text{boundary}}$$


透過這個損失函數，LSTM 將被強制鎖定在具備物理守恆規律的解空間內，BO 將再也無法透過劇烈切換閥門來欺騙模型。

---

#### 四、 帶控制變化率懲罰的複合目標函數 (BO Objective)

若要徹底消除 BO 偏好「高頻、過動閥門訊號」的對抗性漏洞，必須在 BO 評估 PID 參數的目標函數 $J$ 中加入對閥門開度變化率 $\Delta u$ 的嚴厲懲罰：


$$J = \sum_{t=1}^{T} \left( y_{\text{sp}} - \hat{y}_t \right)^2 + \lambda_1 \sum_{t=1}^{T} u_t^2 + \lambda_2 \sum_{t=1}^{T} \left( u_t - u_{t-1} \right)^2$$

* 第一項：設定點追蹤誤差平方和。
* 第二項：控制能量總量懲罰（避免不必要的過度给藥）。
* 第三項：**控制訊號變化率懲罰（懲罰高頻切換與閥門抖動）**。



透過調整懲罰係數 $\lambda_2$，您可以主動限制閥門的機械磨損，強迫 BO 在搜尋時排除所有高頻振盪的「對抗性參數」，選出物理上平滑、穩定且實用的控制增益。
由於 `pbounds` 僅能限制靜態的參數上下界（如 $K_p \in [0.1, 10]$），無法防止 BO 鑽 LSTM 在合法範圍內的預測盲區。因此，必須使用**手動控制迴圈 (Manual Loop)** 來動態拒絕不確定性過高的參數：

```python
from bayes_opt import BayesianOptimization
from bayes_opt import UtilityFunction

# 1. 宣告優化器（不傳入目標函數，手動接管控制流）
optimizer = BayesianOptimization(
    f=None,
    pbounds={"Kp": (0.1, 10.0), "Ki": (0.01, 2.0), "Kd": (0.0, 1.0)},
    random_state=42
)

utility = UtilityFunction(kind="ucb", kappa=1.96, xi=0.01)

# 2. 迭代尋優
for _ in range(50):
    next_point = optimizer.suggest(utility)
    
    # 3. 從底層高斯過程 (GP) 提取不確定性 (預測標準差 std)
    # 我們需要將建議參數轉換為 numpy 陣列進行 predict
    x_array = optimizer._space.params_to_array(next_point).reshape(1, -1)
    
    # 利用內建的 GP 模型預測該點的平均值與標準差
    mean, std = optimizer._gp.predict(x_array, return_std=True)
    
    # 4. 安全審查機制 (Safe BO)
    # 若 std 超過安全臨界值值，代表此參數落入 LSTM 的未知盲區 (OOD)
    if std[0] > 0.5:  # 0.5 為假設的標準差閾值
        # 拒絕此點，直接給予極差的懲罰分數 (例如 -9999)，並註冊回 GP 中
        # 這會教育高斯過程「這是一個危險/無效區域」，下次不要再來探索
        optimizer.register(params=next_point, target=-9999.0)
    else:
        # 5. 安全通過，執行 LSTM 模擬並計算真實適應度
        score = evaluate_pid_on_lstm_simulator(next_point) 
        optimizer.register(params=next_point, target=score)

```

---

#### 三、 物理約束 (PINN) 損失函數設計

為了消除 LSTM 在非自迴歸輸入下對滴定曲線產生的過度平滑效應，並防止模型給出不合物理邏輯的預測，您可以在模型的 Loss 函數中加入**單調性物理約束與一階導數符號限制**：

##### 1. 單調性一階導數限制

酸閥開度（$u_{\text{acid}}$）增加，pH 預測值（$\hat{y}$）必須下降；鹼閥開度（$u_{\text{base}}$）增加，pH 預測值（$\hat{y}$）必須上升。我們將此物理邏輯轉化為偏微分懲罰項並寫入訓練損失中：


$$\mathcal{L}_{\text{physics}} = \max\left(0, \frac{\partial \hat{y}_{t+1}}{\partial u_{\text{acid}, t}}\right) + \max\left(0, -\frac{\partial \hat{y}_{t+1}}{\partial u_{\text{base}, t}}\right)$$

##### 2. 質量守恆與邊界極限

預測的出口 pH 值絕對不可能超出強酸原液的極限酸度 $pH_{\text{min}}$ 與強鹼原液的極限鹼度 $pH_{\text{max}}$：


$$\mathcal{L}_{\text{boundary}} = \max\left(0, pH_{\text{min}} - \hat{y}_{t+1}\right) + \max\left(0, \hat{y}_{t+1} - pH_{\text{max}}\right)$$

##### 3. 最終複合訓練 Loss

$$\mathcal{L}_{\text{total}} = \mathcal{L}_{\text{MSE}} + \alpha \mathcal{L}_{\text{physics}} + \beta \mathcal{L}_{\text{boundary}}$$


透過這個損失函數，LSTM 將被強制鎖定在具備物理守恆規律的解空間內，BO 將再也無法透過劇烈切換閥門來欺騙模型。

---

#### 四、 帶控制變化率懲罰的複合目標函數 (BO Objective)

若要徹底消除 BO 偏好「高頻、過動閥門訊號」的對抗性漏洞，必須在 BO 評估 PID 參數的目標函數 $J$ 中加入對閥門開度變化率 $\Delta u$ 的嚴厲懲罰：


$$J = \sum_{t=1}^{T} \left( y_{\text{sp}} - \hat{y}_t \right)^2 + \lambda_1 \sum_{t=1}^{T} u_t^2 + \lambda_2 \sum_{t=1}^{T} \left( u_t - u_{t-1} \right)^2$$

* 第一項：設定點追蹤誤差平方和。
* 第二項：控制能量總量懲罰（避免不必要的過度给藥）。
* 第三項：**控制訊號變化率懲罰（懲罰高頻切換與閥門抖動）**。



透過調整懲罰係數 $\lambda_2$，您可以主動限制閥門的機械磨損，強迫 BO 在搜尋時排除所有高頻振盪的「對抗性參數」，選出物理上平滑、穩定且實用的控制增益。
透過這個損失函數，LSTM 將被強制鎖定在具備物理守恆規律的解空間內，BO 將再也無法透過劇烈切換閥門來欺騙模型。

---

#### 四、 帶控制變化率懲罰的複合目標函數 (BO Objective)

若要徹底消除 BO 偏好「高頻、過動閥門訊號」的對抗性漏洞，必須在 BO 評估 PID 參數的目標函數 $J$ 中加入對閥門開度變化率 $\Delta u$ 的嚴厲懲罰：


$$J = \sum_{t=1}^{T} \left( y_{\text{sp}} - \hat{y}_t \right)^2 + \lambda_1 \sum_{t=1}^{T} u_t^2 + \lambda_2 \sum_{t=1}^{T} \left( u_t - u_{t-1} \right)^2$$

* 第一項：設定點追蹤誤差平方和。
* 第二項：控制能量總量懲罰（避免不必要的過度给藥）。
* 第三項：**控制訊號變化率懲罰（懲罰高頻切換與閥門抖動）**。



透過調整懲罰係數 $\lambda_2$，您可以主動限制閥門的機械磨損，強迫 BO 在搜尋時排除所有高頻振盪的「對抗性參數」，選出物理上平滑、穩定且實用的控制增益。